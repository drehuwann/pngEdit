{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Lancer",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/output/pngEditor.exe",
            "args": [],
            "cwd": "${fileDirname}",
            "console": "integratedTerminal"
        }
    ]
}
